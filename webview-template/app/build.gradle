apply plugin: 'com.android.application'

android {
    namespace "fun.buildsomething.webview"
    compileSdk 36

    defaultConfig {
        applicationId "__BSF_APPLICATION_ID__"
        minSdk 24
        targetSdk 36
        versionCode __BSF_VERSION_CODE__
        versionName "__BSF_VERSION_NAME__"
    }

    signingConfigs {
        release {
            storeFile file("__BSF_KEYSTORE_PATH__")
            storePassword "__BSF_KEYSTORE_PASSWORD__"
            keyAlias "__BSF_KEY_ALIAS__"
            keyPassword "__BSF_KEY_PASSWORD__"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_21
        targetCompatibility JavaVersion.VERSION_21
    }
}

repositories {
    flatDir {
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'src/main/libs'
    }
}

dependencies {
    implementation project(':capacitor-android')
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'androidx.coordinatorlayout:coordinatorlayout:1.2.0'
    implementation 'androidx.core:core-splashscreen:1.0.1'
}

apply from: 'capacitor.build.gradle'
